<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SampleApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SampleApp" parent="Base.Theme.SampleApp" />

    <style name="DarkRedTheme" parent="DefaultTheme">
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/darkRed</item>
        <item name="colorDisabledDark">@color/darkRedDisabled</item>
        <item name="textColorPrimary">@color/darkRedText</item>
        <item name="colorDisabledSecondary">@color/darkRedDisabledSecondary</item>
    </style>
</resources>